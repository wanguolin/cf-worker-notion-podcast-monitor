# Credentials only. Never commit real values or paste them into issues and logs.
# The two Notion data source IDs are non-sensitive vars in wrangler.jsonc.
NOTION_TOKEN=secret_replace_me
MANUAL_TRIGGER_TOKEN=replace_with_openssl_rand_hex_32
