{
  "name": "cf-worker-notion-podcast-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env finance-production",
    "deploy": "wrangler deploy --env finance-production",
    "test": "vitest run --maxWorkers=1",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "saxes": "^6.0.0"
  }
}
